diff --git a/android/src/main/java/com/sbaiahmed1/reactnativeblur/ReactNativeBlurView.kt b/android/src/main/java/com/sbaiahmed1/reactnativeblur/ReactNativeBlurView.kt
index a42dcc0d5f82927e1224d3156a6ca114c65b92a6..dc049f3e9343de76933433bac1b45628edd56678 100644
--- a/android/src/main/java/com/sbaiahmed1/reactnativeblur/ReactNativeBlurView.kt
+++ b/android/src/main/java/com/sbaiahmed1/reactnativeblur/ReactNativeBlurView.kt
@@ -197,7 +197,7 @@ class ReactNativeBlurView : BlurViewGroup {
   private fun findNearestReactRootView(): ViewGroup? {
     var currentParent = this.parent
     while (currentParent != null) {
-      if (currentParent.javaClass.name == "com.facebook.react.ReactRootView") {
+      if (currentParent is com.facebook.react.ReactRootView) {
         return currentParent as? ViewGroup
       }
       currentParent = currentParent.parent
diff --git a/android/src/main/java/com/sbaiahmed1/reactnativeblur/ReactNativeLiquidGlassView.kt b/android/src/main/java/com/sbaiahmed1/reactnativeblur/ReactNativeLiquidGlassView.kt
index 304d49064f93f91eba1717adcfeb04cec203b1fe..da4211770fbb8fb44477a126fa75a34114b9884b 100644
--- a/android/src/main/java/com/sbaiahmed1/reactnativeblur/ReactNativeLiquidGlassView.kt
+++ b/android/src/main/java/com/sbaiahmed1/reactnativeblur/ReactNativeLiquidGlassView.kt
@@ -59,6 +59,19 @@ class ReactNativeLiquidGlassView(context: Context) : FrameLayout(context) {
   private var borderTopRightRadius = -1f
   private var borderBottomLeftRadius = -1f
   private var borderBottomRightRadius = -1f
+  // Joined shape (see the shader): body below bodyTop, cap blended into it.
+  // All in dp; a cap width of 0 means a plain rounded rect.
+  private var bodyTopDp = 0f
+  private val capRectDp = FloatArray(4)
+  private var capRadiusDp = 0f
+  private var joinSmoothingDp = 12f
+  private var rimAlpha = 0f
+  private val isJoined: Boolean
+    get() = capRectDp[2] > 0f
+  val capX: Float get() = capRectDp[0]
+  val capY: Float get() = capRectDp[1]
+  val capWidth: Float get() = capRectDp[2]
+  val capHeight: Float get() = capRectDp[3]
 
   // Shader engine state; only touched on API 33+.
   private var glassNode: RenderNode? = null
@@ -83,7 +96,12 @@ class ReactNativeLiquidGlassView(context: Context) : FrameLayout(context) {
   // cheap no-op when neither the offset, the capture, nor the effect changed.
   private val scrollTrackingListener = ViewTreeObserver.OnPreDrawListener {
     if (isSupported) {
-      updateGlassNode()
+      try {
+        captureForFrame()
+        updateGlassNode()
+      } catch (e: Exception) {
+        logWarning("pre-draw glass update failed", e)
+      }
     }
     true
   }
@@ -94,29 +112,16 @@ class ReactNativeLiquidGlassView(context: Context) : FrameLayout(context) {
   private val sourceLocation = IntArray(2)
   private val ownLocation = IntArray(2)
 
-  // Attach-scoped throttled backdrop refresh (~30 fps): live enough that
-  // content scrolling behind the glass tracks smoothly, without re-capturing
-  // at full display rate.
-  private val backdropTicker = object : Runnable {
-    override fun run() {
-      refreshBackdrop()
-      postDelayed(this, BACKDROP_REFRESH_MS)
-    }
-  }
-
   companion object {
     private const val TAG = "RNLiquidGlassView"
 
-    private const val BACKDROP_REFRESH_MS = 33L
-
-    // The full-screen software rasterization is the expensive step, so it
-    // runs an order slower than the ticker; each tick only re-blits this
-    // view's sub-rect from the shared bitmap. Both the capture and the
-    // sub-rect are in window coordinates, so glass that scrolls WITH its
-    // content stays pixel-correct against a stale capture (glass and
-    // backdrop move together) — only glass fixed OVER scrolling content
-    // sees the backdrop update at this cadence.
-    private const val SHARED_CAPTURE_MAX_AGE_MS = 100L
+    // The backdrop is re-captured only on frames the window actually renders
+    // (the pre-draw pass runs once per rendered frame and not at all while
+    // the screen is idle), so the glass tracks content at the display's own
+    // refresh rate and costs nothing when nothing moves. The capture is
+    // shared per frame across every glass view on the screen and clipped to
+    // the bands they cover, so a frame's software raster only touches the
+    // pixels the glass can show.
 
     // Capture at third linear resolution: ~9x fewer bytes and ~9x less
     // software raster work than full-res; the shader's blur consumes the
@@ -168,7 +173,7 @@ class ReactNativeLiquidGlassView(context: Context) : FrameLayout(context) {
       val source =
         findNearestScreenAncestor() ?: findNearestReactRootView() ?: parent as? ViewGroup
       captureSource = source
-      source?.let { SharedBackdropCapture.register(it) }
+      source?.let { SharedBackdropCapture.register(it, this) }
       refreshBackdrop()
     }
     initRunnable = runnable
@@ -177,29 +182,6 @@ class ReactNativeLiquidGlassView(context: Context) : FrameLayout(context) {
     registeredTreeObserver = viewTreeObserver.also {
       it.addOnPreDrawListener(scrollTrackingListener)
     }
-    restartTickerIfVisible()
-  }
-
-  // The per-instance software capture is the main running cost, so the ticker
-  // only runs while the view can actually be seen: it stops when the view or
-  // its window is hidden (backgrounded app, covered screen, GONE subtree) and
-  // restarts when visibility returns.
-  private fun restartTickerIfVisible() {
-    if (!constructed) return
-    removeCallbacks(backdropTicker)
-    if (isSupported && isAttachedToWindow && isShown && windowVisibility == VISIBLE) {
-      postDelayed(backdropTicker, BACKDROP_REFRESH_MS)
-    }
-  }
-
-  override fun onWindowVisibilityChanged(visibility: Int) {
-    super.onWindowVisibilityChanged(visibility)
-    restartTickerIfVisible()
-  }
-
-  override fun onVisibilityChanged(changedView: View, visibility: Int) {
-    super.onVisibilityChanged(changedView, visibility)
-    restartTickerIfVisible()
   }
 
   override fun onDetachedFromWindow() {
@@ -210,8 +192,7 @@ class ReactNativeLiquidGlassView(context: Context) : FrameLayout(context) {
   fun cleanup() {
     initRunnable?.let { removeCallbacks(it) }
     initRunnable = null
-    removeCallbacks(backdropTicker)
-    captureSource?.let { SharedBackdropCapture.unregister(it) }
+    captureSource?.let { SharedBackdropCapture.unregister(it, this) }
     captureSource = null
     registeredTreeObserver?.let {
       if (it.isAlive) it.removeOnPreDrawListener(scrollTrackingListener)
@@ -246,7 +227,13 @@ class ReactNativeLiquidGlassView(context: Context) : FrameLayout(context) {
     // directly and would bake the React children into their own backdrop —
     // the shader would then refract the children along with the background.
     if (SharedBackdropCapture.captureInProgress) return
-    if (isSupported && canvas.isHardwareAccelerated && updateGlassNode()) {
+    val drawable = try {
+      isSupported && canvas.isHardwareAccelerated && updateGlassNode()
+    } catch (e: Exception) {
+      logWarning("updateGlassNode failed in draw", e)
+      false
+    }
+    if (drawable) {
       glassNode?.let { canvas.drawRenderNode(it) }
     }
     super.dispatchDraw(canvas)
@@ -263,22 +250,20 @@ class ReactNativeLiquidGlassView(context: Context) : FrameLayout(context) {
     if (shaderFailed || width <= 0 || height <= 0) return false
     val node = glassNode ?: return false
     val source = captureSource ?: return false
-    val bitmap = SharedBackdropCapture.peek(source) ?: return false
+    if (SharedBackdropCapture.peek(source) == null) return false
 
     source.getLocationInWindow(sourceLocation)
     getLocationInWindow(ownLocation)
     val offsetX = ownLocation[0] - sourceLocation[0]
     val offsetY = ownLocation[1] - sourceLocation[1]
 
-    // While this view is moving across the source (scrolling), a stale
-    // capture shows OTHER scrolling content at its old window position
-    // swimming through the glass. Refresh the shared capture at frame
-    // cadence during motion — still one rasterization per interval across
-    // all glass views — and fall back to the relaxed idle cadence
-    // (SHARED_CAPTURE_MAX_AGE_MS, driven by the ticker) once still.
-    if (offsetX != appliedOffsetX || offsetY != appliedOffsetY) {
-      SharedBackdropCapture.acquire(source, CAPTURE_DOWNSAMPLE, BACKDROP_REFRESH_MS)
-    }
+    // The capture for this frame was taken in the pre-draw pass
+    // (captureForFrame); the node just re-blits its sub-rect. A resize of
+    // the capture source (keyboard) may have replaced and recycled the
+    // shared bitmap since it was peeked, and drawing a recycled bitmap
+    // throws and takes the app down.
+    val current = SharedBackdropCapture.peek(source) ?: return false
+    if (current.isRecycled) return false
 
     val generation = SharedBackdropCapture.generationOf(source)
     if (
@@ -303,7 +288,7 @@ class ReactNativeLiquidGlassView(context: Context) : FrameLayout(context) {
       // CAPTURE_DOWNSAMPLE-pixel steps, which reads as the glass "shaking"
       // against smoothly-scrolling content.
       recording.scale(downsample, downsample)
-      recording.drawBitmap(bitmap, -offsetX / downsample, -offsetY / downsample, capturePaint)
+      recording.drawBitmap(current, -offsetX / downsample, -offsetY / downsample, capturePaint)
     } finally {
       node.endRecording()
     }
@@ -329,20 +314,29 @@ class ReactNativeLiquidGlassView(context: Context) : FrameLayout(context) {
     // dispatchDraw skips it entirely.
     if (shaderFailed) return
 
-    // One shared, downsampled capture of the source, re-rasterized at most
-    // every SHARED_CAPTURE_MAX_AGE_MS across every glass view on the screen.
-    // The node itself is re-recorded inside the draw pass (updateGlassNode),
-    // so the ticker only has to trigger a draw when the capture or the
-    // effect actually changed — a fully idle screen therefore stops
-    // invalidating (and stops forcing the window to re-render 30x/s), while
-    // scrolling gets per-frame backdrop tracking from dispatchDraw.
-    SharedBackdropCapture.acquire(source, CAPTURE_DOWNSAMPLE, SHARED_CAPTURE_MAX_AGE_MS)
-      ?: return
+    // Explicit capture outside the frame cadence: first attach and a resize
+    // of this view, where the next rendered frame must not show a stale or
+    // empty backdrop. The node is re-recorded in the draw pass.
+    SharedBackdropCapture.capture(source, CAPTURE_DOWNSAMPLE) ?: return
     if (SharedBackdropCapture.generationOf(source) != appliedGeneration || effectDirty) {
       invalidate()
     }
   }
 
+  /**
+   * Runs in the window's pre-draw pass, i.e. once per frame the window is
+   * about to render. The shared capture rasterizes the source at most once
+   * per frame no matter how many glass views ask, so this is the whole
+   * scheduling policy: content that moves re-renders the window, the glass
+   * follows on the same frame, and an idle screen never captures.
+   */
+  private fun captureForFrame() {
+    if (width <= 0 || height <= 0 || !isAttachedToWindow || shaderFailed) return
+    val source = captureSource ?: return
+    if (source.width <= 0 || source.height <= 0) return
+    SharedBackdropCapture.captureForFrame(source, CAPTURE_DOWNSAMPLE)
+  }
+
   @RequiresApi(Build.VERSION_CODES.TIRAMISU)
   private fun ensureEngine() {
     if (glassNode != null) return
@@ -382,7 +376,23 @@ class ReactNativeLiquidGlassView(context: Context) : FrameLayout(context) {
 
     shader.setFloatUniform("size", floatArrayOf(w, h))
     shader.setFloatUniform("offset", floatArrayOf(0f, 0f))
-    shader.setFloatUniform("cornerRadii", floatArrayOf(radius, radius, radius, radius))
+    // The cap sits flush with the body's right edge, so that body corner is
+    // square and the cap's edge runs straight into the body's.
+    val topRight = if (isJoined) 0f else radius
+    shader.setFloatUniform("cornerRadii", floatArrayOf(radius, topRight, radius, radius))
+    shader.setFloatUniform("bodyTop", if (isJoined) convertDpToPx(bodyTopDp) else 0f)
+    shader.setFloatUniform(
+      "capRect",
+      floatArrayOf(
+        convertDpToPx(capRectDp[0]),
+        convertDpToPx(capRectDp[1]),
+        convertDpToPx(capRectDp[2]),
+        convertDpToPx(capRectDp[3]),
+      ),
+    )
+    shader.setFloatUniform("capRadius", convertDpToPx(capRadiusDp))
+    shader.setFloatUniform("joinSmooth", convertDpToPx(joinSmoothingDp))
+    shader.setFloatUniform("rimAlpha", rimAlpha)
     shader.setFloatUniform("refractionHeight", convertDpToPx(REFRACTION_HEIGHT_DP))
     shader.setFloatUniform("refractionAmount", -convertDpToPx(REFRACTION_OFFSET_DP))
     shader.setFloatUniform("depthEffect", DEPTH_EFFECT)
@@ -401,7 +411,6 @@ class ReactNativeLiquidGlassView(context: Context) : FrameLayout(context) {
       )
     )
     shader.setFloatUniform("tintAlpha", (Color.alpha(glassTintColor) / 255f) * glassOpacity)
-
     val shaderEffect = RenderEffect.createRuntimeShaderEffect(shader, "content")
     val blurRadius =
       if (glassType.equals("regular", ignoreCase = true)) REGULAR_BLUR_RADIUS
@@ -428,7 +437,7 @@ class ReactNativeLiquidGlassView(context: Context) : FrameLayout(context) {
   private fun findNearestReactRootView(): ViewGroup? {
     var currentParent = this.parent
     while (currentParent != null) {
-      if (currentParent.javaClass.name == "com.facebook.react.ReactRootView") {
+      if (currentParent is com.facebook.react.ReactRootView) {
         return currentParent as? ViewGroup
       }
       currentParent = currentParent.parent
@@ -498,6 +507,40 @@ class ReactNativeLiquidGlassView(context: Context) : FrameLayout(context) {
     updateCornerRadius()
   }
 
+  fun setBodyTop(value: Float) {
+    bodyTopDp = value
+    effectDirty = true
+    invalidate()
+  }
+
+  fun setCap(x: Float, y: Float, width: Float, height: Float) {
+    val wasJoined = isJoined
+    capRectDp[0] = x
+    capRectDp[1] = y
+    capRectDp[2] = width
+    capRectDp[3] = height
+    effectDirty = true
+    if (wasJoined != isJoined) updateCornerRadius() else invalidate()
+  }
+
+  fun setCapRadius(value: Float) {
+    capRadiusDp = value
+    effectDirty = true
+    invalidate()
+  }
+
+  fun setJoinSmoothing(value: Float) {
+    joinSmoothingDp = value
+    effectDirty = true
+    invalidate()
+  }
+
+  fun setRimAlpha(value: Float) {
+    rimAlpha = value.coerceIn(0f, 1f)
+    effectDirty = true
+    invalidate()
+  }
+
   fun setBorderTopLeftRadius(radius: Float) {
     borderTopLeftRadius = radius
     updateCornerRadius()
@@ -543,6 +586,16 @@ class ReactNativeLiquidGlassView(context: Context) : FrameLayout(context) {
     // outline clip for their exact shape.
     shaderRadiusPx = if (isUniform) topLeft else baseRadius
 
+    if (isJoined) {
+      // The union of body and cap is not an outline shape; the shader masks
+      // it, so the view does not clip at all.
+      outlineProvider = ViewOutlineProvider.BOUNDS
+      clipToOutline = false
+      invalidateOutline()
+      effectDirty = true
+      invalidate()
+      return
+    }
     if (isUniform) {
       outlineProvider = object : ViewOutlineProvider() {
         override fun getOutline(view: View, outline: Outline?) {
diff --git a/android/src/main/java/com/sbaiahmed1/reactnativeblur/ReactNativeLiquidGlassViewManager.kt b/android/src/main/java/com/sbaiahmed1/reactnativeblur/ReactNativeLiquidGlassViewManager.kt
index df28970916f61e522b3a740deda8af498ea2f397..72a3253bba53cbd1e863751582cf939edd18a3d6 100644
--- a/android/src/main/java/com/sbaiahmed1/reactnativeblur/ReactNativeLiquidGlassViewManager.kt
+++ b/android/src/main/java/com/sbaiahmed1/reactnativeblur/ReactNativeLiquidGlassViewManager.kt
@@ -49,6 +49,46 @@ class ReactNativeLiquidGlassViewManager : ViewGroupManager<ReactNativeLiquidGlas
     view?.setIsInteractive(isInteractive)
   }
 
+  @ReactProp(name = "bodyTop")
+  override fun setBodyTop(view: ReactNativeLiquidGlassView?, value: Double) {
+    view?.setBodyTop(value.toFloat())
+  }
+
+  @ReactProp(name = "capX")
+  override fun setCapX(view: ReactNativeLiquidGlassView?, value: Double) {
+    view?.let { it.setCap(value.toFloat(), it.capY, it.capWidth, it.capHeight) }
+  }
+
+  @ReactProp(name = "capY")
+  override fun setCapY(view: ReactNativeLiquidGlassView?, value: Double) {
+    view?.let { it.setCap(it.capX, value.toFloat(), it.capWidth, it.capHeight) }
+  }
+
+  @ReactProp(name = "capWidth")
+  override fun setCapWidth(view: ReactNativeLiquidGlassView?, value: Double) {
+    view?.let { it.setCap(it.capX, it.capY, value.toFloat(), it.capHeight) }
+  }
+
+  @ReactProp(name = "capHeight")
+  override fun setCapHeight(view: ReactNativeLiquidGlassView?, value: Double) {
+    view?.let { it.setCap(it.capX, it.capY, it.capWidth, value.toFloat()) }
+  }
+
+  @ReactProp(name = "capRadius")
+  override fun setCapRadius(view: ReactNativeLiquidGlassView?, value: Double) {
+    view?.setCapRadius(value.toFloat())
+  }
+
+  @ReactProp(name = "joinSmoothing")
+  override fun setJoinSmoothing(view: ReactNativeLiquidGlassView?, value: Double) {
+    view?.setJoinSmoothing(value.toFloat())
+  }
+
+  @ReactProp(name = "rimAlpha")
+  override fun setRimAlpha(view: ReactNativeLiquidGlassView?, value: Double) {
+    view?.setRimAlpha(value.toFloat())
+  }
+
   @ReactProp(name = "reducedTransparencyFallbackColor")
   override fun setReducedTransparencyFallbackColor(view: ReactNativeLiquidGlassView?, reducedTransparencyFallbackColor: String?) {
     // no-op
diff --git a/android/src/main/java/com/sbaiahmed1/reactnativeblur/SharedBackdropCapture.kt b/android/src/main/java/com/sbaiahmed1/reactnativeblur/SharedBackdropCapture.kt
index c15ec61def88ce869f8aea3a774edae52ad4df35..539a313ba5e81e3dfa3ab5d92707fa5e4f1f8e9e 100644
--- a/android/src/main/java/com/sbaiahmed1/reactnativeblur/SharedBackdropCapture.kt
+++ b/android/src/main/java/com/sbaiahmed1/reactnativeblur/SharedBackdropCapture.kt
@@ -2,8 +2,9 @@ package com.sbaiahmed1.reactnativeblur
 
 import android.graphics.Bitmap
 import android.graphics.Canvas
-import android.os.SystemClock
+import android.graphics.Rect
 import android.view.View
+import android.view.animation.AnimationUtils
 
 /**
  * Deduplicates the software backdrop captures behind ReactNativeLiquidGlassView.
@@ -22,13 +23,25 @@ internal object SharedBackdropCapture {
   private class Entry {
     var bitmap: Bitmap? = null
     var canvas: Canvas? = null
-    var lastCaptureUptimeMs = 0L
-    var refCount = 0
+    // Animation-clock time of the frame the last capture was taken for; every
+    // pre-draw listener in one frame sees the same value, so it dedupes the
+    // per-frame capture across glass views without a timer.
+    var lastFrameTimeMs = -1L
+    // The glass views bound to this source; the capture is clipped to the
+    // bands they cover.
+    val consumers = ArrayList<View>()
     // Bumped on every successful capture so consumers can cheaply detect
     // whether the shared bitmap changed since they last consumed it.
     var generation = 0L
   }
 
+  // Refraction and blur read this far outside a glass view's own rect.
+  private const val CAPTURE_MARGIN_PX = 64
+
+  private val sourceLocation = IntArray(2)
+  private val consumerLocation = IntArray(2)
+  private val bands = ArrayList<Rect>()
+
   private val entries = HashMap<View, Entry>()
 
   /**
@@ -40,26 +53,52 @@ internal object SharedBackdropCapture {
   var captureInProgress = false
     private set
 
-  fun register(source: View) {
+  fun register(source: View, consumer: View) {
     val entry = entries.getOrPut(source) { Entry() }
-    entry.refCount++
+    if (entry.consumers.contains(consumer)) return
+    entry.consumers.add(consumer)
+    // The frame's capture may already have run for the other glass views,
+    // without a band for this one; a menu opening over a static screen would
+    // then read an area that was never rasterized and draw as a flat tint
+    // until something else repaints. Forget the frame so the next request
+    // captures its band too.
+    entry.lastFrameTimeMs = -1L
   }
 
-  fun unregister(source: View) {
+  fun unregister(source: View, consumer: View) {
     val entry = entries[source] ?: return
-    entry.refCount--
-    if (entry.refCount <= 0) {
+    entry.consumers.remove(consumer)
+    if (entry.consumers.isEmpty()) {
       entry.bitmap?.recycle()
       entries.remove(source)
     }
   }
 
   /**
-   * Returns the shared downsampled capture of [source], re-rasterizing only
-   * when the cached one is older than [maxAgeMs] — so any number of glass
-   * views refreshing in the same interval share a single capture.
+   * The shared capture for the frame currently being prepared: rasterizes
+   * [source] once per frame however many glass views call this from their
+   * pre-draw listeners, and returns the cached bitmap for the rest.
+   */
+  fun captureForFrame(source: View, downsample: Int): Bitmap? {
+    val entry = entries[source] ?: return null
+    val frameTime = AnimationUtils.currentAnimationTimeMillis()
+    val bitmap = entry.bitmap
+    if (
+      bitmap != null &&
+      entry.lastFrameTimeMs == frameTime &&
+      bitmap.width == (source.width / downsample).coerceAtLeast(1) &&
+      bitmap.height == (source.height / downsample).coerceAtLeast(1)
+    ) {
+      return bitmap
+    }
+    return capture(source, downsample, frameTime)
+  }
+
+  /**
+   * Rasterizes [source] now into the shared downsampled bitmap, clipped to
+   * the bands the bound glass views cover (plus a margin for refraction).
    */
-  fun acquire(source: View, downsample: Int, maxAgeMs: Long): Bitmap? {
+  fun capture(source: View, downsample: Int, frameTimeMs: Long = -1L): Bitmap? {
     val entry = entries[source] ?: return null
     if (source.width <= 0 || source.height <= 0) return null
 
@@ -72,39 +104,69 @@ internal object SharedBackdropCapture {
       bitmap = Bitmap.createBitmap(bitmapWidth, bitmapHeight, Bitmap.Config.ARGB_8888)
       entry.bitmap = bitmap
       entry.canvas = Canvas(bitmap)
-      entry.lastCaptureUptimeMs = 0L
-    }
-
-    val now = SystemClock.uptimeMillis()
-    if (!sizeChanged && now - entry.lastCaptureUptimeMs < maxAgeMs) {
-      return bitmap
     }
 
     val canvas = entry.canvas ?: return bitmap
-    bitmap!!.eraseColor(0)
-    val save = canvas.save()
-    canvas.scale(1f / downsample, 1f / downsample)
+    collectBands(source, entry)
     captureInProgress = true
     var captured = true
     try {
-      source.draw(canvas)
+      for (band in bands) {
+        val save = canvas.save()
+        canvas.scale(1f / downsample, 1f / downsample)
+        canvas.clipRect(band)
+        // Only the band is redrawn; pixels outside it keep the previous
+        // frame's content, which is still correct there for the views that
+        // read it.
+        canvas.drawColor(0, android.graphics.PorterDuff.Mode.CLEAR)
+        source.draw(canvas)
+        canvas.restoreToCount(save)
+      }
     } catch (_: Exception) {
-      // A view that throws mid-draw leaves a partial backdrop for one tick.
+      // A view that throws mid-draw leaves a partial backdrop for one frame.
       captured = false
     } finally {
       captureInProgress = false
-      canvas.restoreToCount(save)
     }
     // Only a successful draw counts as fresh — a partial capture keeps the
-    // stale timestamp so the very next acquire re-rasterizes instead of
-    // serving the broken frame for the whole interval.
+    // stale frame stamp so the next request re-rasterizes instead of serving
+    // the broken frame.
     if (captured) {
-      entry.lastCaptureUptimeMs = now
+      entry.lastFrameTimeMs = frameTimeMs
       entry.generation++
     }
     return bitmap
   }
 
+  /**
+   * Full-width horizontal bands (in source pixels) covering every bound
+   * glass view plus the refraction margin, overlapping bands merged, so the
+   * software raster skips everything the glass cannot show. Header pills and
+   * a bottom composer are two thin bands instead of the whole screen.
+   */
+  private fun collectBands(source: View, entry: Entry) {
+    bands.clear()
+    source.getLocationInWindow(sourceLocation)
+    for (consumer in entry.consumers) {
+      if (consumer.width <= 0 || consumer.height <= 0 || !consumer.isShown) continue
+      consumer.getLocationInWindow(consumerLocation)
+      val top = consumerLocation[1] - sourceLocation[1] - CAPTURE_MARGIN_PX
+      val bottom = consumerLocation[1] - sourceLocation[1] + consumer.height + CAPTURE_MARGIN_PX
+      val band = Rect(0, top.coerceAtLeast(0), source.width, bottom.coerceAtMost(source.height))
+      if (band.isEmpty) continue
+      var merged = false
+      for (existing in bands) {
+        if (band.top <= existing.bottom && band.bottom >= existing.top) {
+          existing.union(band)
+          merged = true
+          break
+        }
+      }
+      if (!merged) bands.add(band)
+    }
+    if (bands.isEmpty()) bands.add(Rect(0, 0, source.width, source.height))
+  }
+
   /** Generation of the last successful capture for [source]; consumers use it
    * to skip re-recording when neither the bitmap nor their sub-rect moved. */
   fun generationOf(source: View): Long = entries[source]?.generation ?: 0L
diff --git a/android/src/main/res/raw/rnblur_liquidglass_effect.agsl b/android/src/main/res/raw/rnblur_liquidglass_effect.agsl
index cce751fc18a2f6ca174249060971c267f90ec122..fa2ff1eb73e3d7678401e0c61942f6558315592d 100644
--- a/android/src/main/res/raw/rnblur_liquidglass_effect.agsl
+++ b/android/src/main/res/raw/rnblur_liquidglass_effect.agsl
@@ -49,6 +49,17 @@ uniform float chromaMultiplier;
 uniform float3 tintColor;
 uniform float tintAlpha;
 
+// Optional second lobe joined to the body as one piece of glass (a tab on
+// the composer). The body is the view below bodyTop; capRect (x, y, w, h,
+// view px, w <= 0 = none) is a rounded rect blended into it with a smooth
+// minimum, so the edge refraction runs around the union and the join reads
+// as one meniscus instead of two overlapping panes.
+uniform float bodyTop;
+uniform float4 capRect;
+uniform float capRadius;
+uniform float joinSmooth;
+uniform float rimAlpha;
+
 const half3 rgbToY = half3(0.2126, 0.7152, 0.0722);
 
 float radiusAt(float2 coord, float4 radii) {
@@ -99,6 +110,24 @@ float2 gradSdRoundedRect(float2 coord, float2 halfSize, float radius) {
     }
 }
 
+float smoothMin(float a, float b, float k) {
+    float h = max(k - abs(a - b), 0.0) / k;
+    return min(a, b) - h * h * k * 0.25;
+}
+
+// Signed distance to the glass shape in view pixels: the body alone, or the
+// body joined with the cap.
+float sdShape(float2 p) {
+    float2 bodyHalf = float2(size.x, size.y - bodyTop) * 0.5;
+    float2 bodyCoord = p - float2(size.x * 0.5, bodyTop + bodyHalf.y);
+    float sdBody = sdRoundedRect(bodyCoord, bodyHalf, radiusAt(bodyCoord, cornerRadii));
+    if (capRect.z <= 0.0) return sdBody;
+    float2 capHalf = capRect.zw * 0.5;
+    float2 capCoord = p - (capRect.xy + capHalf);
+    float sdCap = sdRoundedRect(capCoord, capHalf, min(capRadius, min(capHalf.x, capHalf.y)));
+    return smoothMin(sdBody, sdCap, max(joinSmooth, 0.001));
+}
+
 float circleMap(float x) {
     return 1.0 - sqrt(1.0 - x * x);
 }
@@ -115,8 +144,16 @@ half4 main(float2 coord) {
     float2 halfSize = size * 0.5;
     float2 centeredCoord = (coord + offset) - halfSize;
     float radius = radiusAt(centeredCoord, cornerRadii);
-
-    float sd = sdRoundedRect(centeredCoord, halfSize, radius);
+    bool joined = capRect.z > 0.0;
+
+    float2 shapeCoord = coord + offset;
+    float sd = sdShape(shapeCoord);
+    // Outside the joined shape the view is a plain rectangle (no outline
+    // clip can follow the union), so the shader cuts the shape itself.
+    if (joined && sd > 0.75) {
+        return half4(0.0);
+    }
+    float mask = joined ? 1.0 - smoothstep(-0.75, 0.75, sd) : 1.0;
     if (-sd >= refractionHeight) {
         half4 baseColor = content.eval(coord);
 
@@ -134,8 +171,22 @@ half4 main(float2 coord) {
     float smoothRadius = max(radius * 1.5, 30.0);
     float gradRadius = min(smoothRadius, min(halfSize.x, halfSize.y));
 
-    float2 shapeGrad = gradSdRoundedRect(centeredCoord, halfSize, gradRadius);
-    float2 depthGrad = safeNormalize(centeredCoord, shapeGrad);
+    float2 shapeGrad;
+    float2 depthCoord = centeredCoord;
+    if (joined) {
+        // Numerical gradient of the union; the wide step softens the lens the
+        // way the enlarged analytic radius does for a lone rounded rect.
+        float e = 4.0;
+        float2 delta = float2(
+            sdShape(shapeCoord + float2(e, 0.0)) - sdShape(shapeCoord - float2(e, 0.0)),
+            sdShape(shapeCoord + float2(0.0, e)) - sdShape(shapeCoord - float2(0.0, e))
+        );
+        shapeGrad = safeNormalize(delta, float2(0.0, -1.0));
+        depthCoord = shapeCoord - float2(size.x * 0.5, bodyTop + (size.y - bodyTop) * 0.5);
+    } else {
+        shapeGrad = gradSdRoundedRect(centeredCoord, halfSize, gradRadius);
+    }
+    float2 depthGrad = safeNormalize(depthCoord, shapeGrad);
     float2 grad = safeNormalize(shapeGrad + depthEffect * depthGrad, shapeGrad);
 
     float2 refractedCoord = coord + d * grad;
@@ -182,5 +233,11 @@ half4 main(float2 coord) {
     color.rgb = (color.rgb - 0.5) * (1.0 + contrast) + 0.5;
 
     half3 tintedRGB = mix(color.rgb, tintColor, tintAlpha);
-    return half4(tintedRGB, color.a);
+    if (joined) {
+        // A faint rim just inside the edge, where a bordered view would draw
+        // its outline; it follows the union exactly.
+        float rim = rimAlpha * (1.0 - smoothstep(0.0, 1.5, -sd));
+        tintedRGB = mix(tintedRGB, half3(1.0), rim);
+    }
+    return half4(tintedRGB, color.a) * mask;
 }
diff --git a/lib/module/ReactNativeLiquidGlassViewNativeComponent.ts b/lib/module/ReactNativeLiquidGlassViewNativeComponent.ts
index 343fb7481dd887ebbcd11ad3b71acae2adb44630..bdaf9c067ca7ba553f262bfe651fc14a10a1c9ab 100644
--- a/lib/module/ReactNativeLiquidGlassViewNativeComponent.ts
+++ b/lib/module/ReactNativeLiquidGlassViewNativeComponent.ts
@@ -13,6 +13,16 @@ export interface NativeProps extends ViewProps {
   >;
   isInteractive?: CodegenTypes.WithDefault<boolean, true>;
   ignoreSafeArea?: CodegenTypes.WithDefault<boolean, true>;
+  // Android: a second rounded rect (the cap, in dp) joined to the body below
+  // bodyTop as one piece of glass. capWidth 0 leaves a plain rounded rect.
+  bodyTop?: CodegenTypes.WithDefault<CodegenTypes.Double, 0.0>;
+  capX?: CodegenTypes.WithDefault<CodegenTypes.Double, 0.0>;
+  capY?: CodegenTypes.WithDefault<CodegenTypes.Double, 0.0>;
+  capWidth?: CodegenTypes.WithDefault<CodegenTypes.Double, 0.0>;
+  capHeight?: CodegenTypes.WithDefault<CodegenTypes.Double, 0.0>;
+  capRadius?: CodegenTypes.WithDefault<CodegenTypes.Double, 0.0>;
+  joinSmoothing?: CodegenTypes.WithDefault<CodegenTypes.Double, 12.0>;
+  rimAlpha?: CodegenTypes.WithDefault<CodegenTypes.Double, 0.0>;
 }
 
 export default codegenNativeComponent<NativeProps>(
diff --git a/lib/typescript/src/ReactNativeLiquidGlassViewNativeComponent.d.ts b/lib/typescript/src/ReactNativeLiquidGlassViewNativeComponent.d.ts
index 28b042f1a1440702270832c7b83162815a3552c1..ab13a1bb0087c269c2cbae6d49cf1f22c487d768 100644
--- a/lib/typescript/src/ReactNativeLiquidGlassViewNativeComponent.d.ts
+++ b/lib/typescript/src/ReactNativeLiquidGlassViewNativeComponent.d.ts
@@ -7,6 +7,14 @@ export interface NativeProps extends ViewProps {
     reducedTransparencyFallbackColor?: CodegenTypes.WithDefault<string, '#FFFFFF'>;
     isInteractive?: CodegenTypes.WithDefault<boolean, true>;
     ignoreSafeArea?: CodegenTypes.WithDefault<boolean, true>;
+    bodyTop?: CodegenTypes.WithDefault<CodegenTypes.Double, 0.0>;
+    capX?: CodegenTypes.WithDefault<CodegenTypes.Double, 0.0>;
+    capY?: CodegenTypes.WithDefault<CodegenTypes.Double, 0.0>;
+    capWidth?: CodegenTypes.WithDefault<CodegenTypes.Double, 0.0>;
+    capHeight?: CodegenTypes.WithDefault<CodegenTypes.Double, 0.0>;
+    capRadius?: CodegenTypes.WithDefault<CodegenTypes.Double, 0.0>;
+    joinSmoothing?: CodegenTypes.WithDefault<CodegenTypes.Double, 12.0>;
+    rimAlpha?: CodegenTypes.WithDefault<CodegenTypes.Double, 0.0>;
 }
 declare const _default: import("react-native").HostComponent<NativeProps>;
 export default _default;
diff --git a/src/ReactNativeLiquidGlassViewNativeComponent.ts b/src/ReactNativeLiquidGlassViewNativeComponent.ts
index 343fb7481dd887ebbcd11ad3b71acae2adb44630..bdaf9c067ca7ba553f262bfe651fc14a10a1c9ab 100644
--- a/src/ReactNativeLiquidGlassViewNativeComponent.ts
+++ b/src/ReactNativeLiquidGlassViewNativeComponent.ts
@@ -13,6 +13,16 @@ export interface NativeProps extends ViewProps {
   >;
   isInteractive?: CodegenTypes.WithDefault<boolean, true>;
   ignoreSafeArea?: CodegenTypes.WithDefault<boolean, true>;
+  // Android: a second rounded rect (the cap, in dp) joined to the body below
+  // bodyTop as one piece of glass. capWidth 0 leaves a plain rounded rect.
+  bodyTop?: CodegenTypes.WithDefault<CodegenTypes.Double, 0.0>;
+  capX?: CodegenTypes.WithDefault<CodegenTypes.Double, 0.0>;
+  capY?: CodegenTypes.WithDefault<CodegenTypes.Double, 0.0>;
+  capWidth?: CodegenTypes.WithDefault<CodegenTypes.Double, 0.0>;
+  capHeight?: CodegenTypes.WithDefault<CodegenTypes.Double, 0.0>;
+  capRadius?: CodegenTypes.WithDefault<CodegenTypes.Double, 0.0>;
+  joinSmoothing?: CodegenTypes.WithDefault<CodegenTypes.Double, 12.0>;
+  rimAlpha?: CodegenTypes.WithDefault<CodegenTypes.Double, 0.0>;
 }
 
 export default codegenNativeComponent<NativeProps>(
